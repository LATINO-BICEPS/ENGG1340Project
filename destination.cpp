// destination.cpp

#include "destination.h"
#include "gameTexts.h"
#include <string>

using namespace std;

/* Dest::Dest(string x1, string x2, bool x3) {
    name = x1;
    description = x2;
    isEnd = x3;
}

Dest::Dest() {
    name = "Default";
    description = "Uninitialized";
    isEnd = false;
} 

void Dest::printDes() {
            std::string toPrint1 = "Welcome to " + name;
            printText(toPrint1);
            printText(description);
} */